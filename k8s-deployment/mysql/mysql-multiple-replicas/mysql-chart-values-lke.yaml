architecture: replication
auth:
  rootPassword: my-secret-pass
  database: my-app-db
  username: my-user
  password: my-pass

primary:
  persistence:
    storageClass: linode-block-storage

secondary:
  replicaCount: 2
  persistence:
    storageClass: linode-block-storage

volumePermissions:
  enabled: true
